# Local .terraform directories (plugins, modules)
**/.terraform/*
.terraform/
terraform.tfvars
 
# State files (CRITICAL: Never commit these)
*.tfstate
*.tfstate.*
 
# Sensitive Variable files
*.tfvars
*.tfvars.json
 
# Crash log files
crash.log
crash.*.log
 
# CLI configuration files
.terraformrc
terraform.rc
 
# Plan output files (binary files generated by plan -out)
*.tfplan
 
# Ignore override files (used for local overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json
 
#Ignore the ssh & pem keys
*.pem
ssh-key-*